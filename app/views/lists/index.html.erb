<div class="container mt-5">
  <h1>Lists</h1>


  <ul>
    <% @lists.each do |list| %>
      <li>
        <%= link_to list.name, list_path(list) %>
      </li>
    <% end %>
  </ul>

  <i class="fa-solid fa-plus"></i> <%= link_to 'Create a new list', new_list_path %>
</div>
